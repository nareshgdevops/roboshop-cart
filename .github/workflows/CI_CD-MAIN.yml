name: CI/CD - MAIN
on:
  workflow_dispatch:
  push:
    branches:
      - 'main'
jobs:
  Build:
    uses: nareshgdevops/roboshop-github-actions/.github/workflows/CI-reusable.yml@main
    with:
      appType: nodejs
